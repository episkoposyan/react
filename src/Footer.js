import React from 'react';
import footer from './footer.module.css';

const Footer = () => {
    return (
        <footer>
            ARtush
        </footer>
    );
};

export default Footer;